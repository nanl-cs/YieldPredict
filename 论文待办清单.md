# 尚需运行的代码任务清单

本文档只保留**仍需实际运行代码、重新训练模型或导出结果**的任务。论文改写、补充公式、调整术语、制作文字说明和投稿排版不列入此清单。

## 1. 当前判断

现有代码已经覆盖数据拼接、特征工程、P1--P5基础实验、P4多目标优化、多模型比较、数据源消融和SHAP分析。当前不需要从头重跑全部研究，最优先补充的是：

1. 派生特征数值诊断；
2. 原始特征与派生特征敏感性对照；
3. 关键结果的逐年与分区稳定性检查；
4. 现有结果与代码的一致性复核。

---

## 2. P0：必须补跑的代码任务

### 2.1 派生特征数值诊断

**目的：** 判断除法型派生特征是否产生极端值，以及模型是否可能受到少量异常样本支配。

**建议基于：** `CodeUse/01拼数据+数据处理.ipynb`生成的最终建模数据。

- [ ] 对以下变量按P1--P5分别统计：样本数、缺失数、零值数、均值、标准差、最小值、1%、5%、25%、50%、75%、95%、99%和最大值：
  - `WUE`；
  - `Decoupling_Stress`；
  - `Thermal_Efficiency`；
  - `Hydrothermal_Balance`；
  - `Drought_Vulnerability`；
  - `Fertility_Vigor`；
  - `Cum_VPD`；
  - `Delta_SM`、`Delta_NDVI`和`Delta_NDWI`。
- [ ] 按年份分别统计上述分位数，检查某一年是否出现异常尺度。
- [ ] 按Zone分别统计上述分位数，检查异常值是否集中于特定区域。
- [ ] 统计每个阶段`SM`接近0的样本数，例如`SM < 0.01`、`SM < 0.02`。
- [ ] 统计每个阶段`Tmean <= 0`的样本数；这些样本在代码中使用`0.1`作为分母。
- [ ] 统计`Clay + 0.1`的实际取值范围，确认Clay进入公式时的单位和缩放尺度。
- [ ] 输出派生特征之间以及派生特征与原始构成变量之间的Spearman相关矩阵。
- [ ] 为六个复合代理特征输出直方图或对数尺度直方图、箱线图和按年份箱线图。

**必须输出：**

- [ ] `derived_feature_summary_overall.csv`
- [ ] `derived_feature_summary_by_year.csv`
- [ ] `derived_feature_summary_by_zone.csv`
- [ ] `derived_feature_edge_case_counts.csv`
- [ ] `derived_feature_spearman.csv`
- [ ] 派生特征分布图文件夹

**完成标准：** 能明确判断各派生特征是否存在异常长尾、除零附近放大、年份尺度漂移或分区尺度不一致。

### 2.2 原始特征与派生特征敏感性实验

**目的：** 验证这些派生特征是否真正改善跨年预测，而不是仅在SHAP中表现重要。

**统一要求：**

- [ ] 使用同一份P4数据。
- [ ] 使用完全相同的外层留一年验证划分。
- [ ] 使用完全相同的模型类、固定参数或相同Optuna搜索预算。
- [ ] 固定随机种子42。
- [ ] 所有预处理、特征筛选和调参只能在训练年份内部完成。

**至少运行以下三组：**

- [ ] `Raw`：仅使用原始变量和三项土壤变量，不含任何人为派生特征。
  - Soil：Sand、Clay、SOC；
  - Meteorology：Tmean、PPT、SM、GDD、FDD、VPD、VPD_max；
  - RS：NDVI、NDVI_max、NDWI、NIRv、EVI、EVI_max。
- [ ] `Raw+Cumulative+Delta`：在Raw基础上加入累积量和阶段差分，但不加入六个复合比值或交互项。
- [ ] `Full`：使用当前论文模型的全部特征，包括六个复合代理特征。

**建议增加的诊断组：**

- [ ] `Full-WUE`：从Full删除所有阶段WUE。
- [ ] `Full-Hydrothermal`：从Full删除所有Hydrothermal_Balance。
- [ ] `Full-DroughtVulnerability`：从Full删除所有Drought_Vulnerability。
- [ ] `Full-AllRatioProxies`：删除WUE、Decoupling_Stress、Thermal_Efficiency、Hydrothermal_Balance和Drought_Vulnerability，仅保留Fertility_Vigor交互项。

**每组必须计算：**

- [ ] 每个测试年份的MAPE、RRMSE、RMSE、MAE、$R^2$和d-index。
- [ ] 六个测试年份预测值合并后的总体指标。
- [ ] 每个测试年份的样本数。
- [ ] 每个外层折的最佳参数和入选特征数；若使用固定参数，则记录固定参数。
- [ ] 每个样本的Year、Zone、真实值、预测值和误差。

**必须输出：**

- [ ] `derived_feature_sensitivity_by_year.csv`
- [ ] `derived_feature_sensitivity_overall.csv`
- [ ] `derived_feature_sensitivity_predictions.csv`
- [ ] `derived_feature_sensitivity_best_params.csv`
- [ ] `derived_feature_sensitivity_selected_features.json`

**结果决策标准：**

- Full在多数年份稳定优于Raw，才支持“派生特征提高预测性能”。
- 只有合并总体指标改善、但多数单年没有改善时，只能认为增益不稳定。
- Full与Raw差异极小时，优先采用更简单、更容易解释的特征体系。
- 删除某个代理特征后性能不降或反而提升时，应考虑将该特征从主模型移除。

### 2.3 派生特征极端值处理敏感性实验

**目的：** 判断模型表现是否依赖代理特征中的极端值。

在任务2.1确认存在明显长尾后，至少比较以下方案：

- [ ] `Original`：保持当前代码不变。
- [ ] `Winsorized`：仅根据每个外层训练集的1%和99%分位数截尾，再应用到测试年。
- [ ] `Remove-risky`：删除存在明显数值风险的比值特征。

**注意：** 截尾阈值必须只由训练年份计算，不能使用测试年份分布。

**必须输出：**

- [ ] `proxy_outlier_sensitivity_by_year.csv`
- [ ] `proxy_outlier_sensitivity_overall.csv`
- [ ] `proxy_outlier_sensitivity_predictions.csv`

**完成标准：** 如果截尾或删除高风险特征后结果基本不变，可以证明主结论不依赖极端值；如果结果变化明显，则需确定新的最终特征处理方案并重跑后续模型。

---

## 3. P1：必须核对并视结果决定是否重跑

### 3.1 复核P1--P5基础实验

**现有代码：** `CodeUse/03减少静态.ipynb`

- [ ] 确认输入数据是最终版本，且只保留Sand、Clay、SOC三项静态变量。
- [ ] 确认实际使用`XGBRFRegressor`，基础参数与论文一致。
- [ ] 确认`Year`、`Zone`、经纬度没有进入预测特征。
- [ ] 确认P1--P5仅使用截至当前阶段的变量。
- [ ] 确认测试年份从训练和所有数据驱动处理步骤中完全排除。
- [ ] 重新运行并导出逐年结果，而不仅是总体结果。
- [ ] 将新结果与`data/Progressive_LOYO_Overall_Results.csv`逐项比较。

**必须输出：**

- [ ] `Progressive_LOYO_ByYear_Results.csv`
- [ ] `Progressive_LOYO_Overall_Results_reproduced.csv`
- [ ] `Progressive_LOYO_Predictions.csv`

**仅在以下情况重跑全部后续实验：** 基础结果不能复现、输入特征发生变化、缺失值处理发生变化，或派生特征诊断后决定修改特征工程。

### 3.2 复核P4 MOBO优化实验

**现有代码：** `CodeUse/optuna_MOBO.ipynb`

- [ ] 确认外层每次留出一个完整年份。
- [ ] 确认内层`LeaveOneGroupOut`只使用外层训练年份。
- [ ] 确认测试年份未参与特征掩码选择、参数优化或模型选择。
- [ ] 确认每个外层折运行50次Optuna试验。
- [ ] 确认最终方案来自Pareto前沿，并按内部MAE选择。
- [ ] 导出每个测试年份的最佳参数、入选特征、内部MAE和特征数。
- [ ] 导出逐样本预测值，以便重新计算全部指标。

**必须输出：**

- [ ] `P4_MOBO_ByYear_Results.csv`
- [ ] `P4_MOBO_Overall_Results.csv`
- [ ] `P4_MOBO_Predictions.csv`
- [ ] `P4_MOBO_Best_Params.csv`
- [ ] `P4_MOBO_Selected_Features.json`
- [ ] `P4_MOBO_Pareto_Solutions.csv`

### 3.3 复核多模型比较

**现有代码：** `CodeUse/model_comparison.ipynb`

- [ ] 确认MLR、SVR、XGBoost和RF+MOBO使用相同外层测试年份。
- [ ] 确认各模型的标准化、特征筛选和调参均仅在训练年份完成。
- [ ] 确认SVR的标准化没有在完整数据上预先拟合。
- [ ] 确认MLR特征筛选没有查看测试年。
- [ ] 确认XGBoost与RF+MOBO均导出实际最佳参数。
- [ ] 导出逐年指标和逐样本预测，不只保留总体汇总。

**必须输出：**

- [ ] `P4_Model_Comparison_ByYear.csv`
- [ ] `P4_Model_Comparison_Overall.csv`
- [ ] `P4_Model_Comparison_Predictions.csv`
- [ ] `P4_Model_Comparison_Best_Params.csv`
- [ ] `P4_Model_Comparison_Selected_Features.json`

### 3.4 复核数据源消融实验

**现有代码：** `results_消融实验/消融.ipynb`

- [ ] 确认该实验使用`RandomForestRegressor`而非`XGBRFRegressor`。
- [ ] 确认所有数据源组合使用完全一致的固定参数。
- [ ] 确认所有组合使用相同样本和相同年份划分。
- [ ] 检查`Fertility_Vigor=NDVI×SOC`当前被归入Meteorology组是否合理；它同时使用遥感和土壤变量，可能造成单源消融组边界不纯。
- [ ] 检查WUE、Thermal_Efficiency等同时含遥感变量的派生特征是否被归入Meteorology组；如是，Meteorology组实际上不是纯气象组。
- [ ] 根据上述检查决定是否需要重新定义为：
  - Soil原始变量；
  - Meteorology原始变量；
  - RS原始变量；
  - Derived代理特征；
  - All全量变量。
- [ ] 如果保留原四组，必须至少补跑一个“原始单源变量”消融版本，避免跨数据源派生特征污染单源比较。

**建议的新消融设计：**

- [ ] `Soil-Raw`
- [ ] `Meteorology-Raw`
- [ ] `RS-Raw`
- [ ] `Raw-All`
- [ ] `Derived-Only`
- [ ] `Full-All`

**必须输出：**

- [ ] `rf_ablation_revised_by_year.csv`
- [ ] `rf_ablation_revised_overall.csv`
- [ ] `rf_ablation_revised_predictions.csv`
- [ ] `rf_ablation_revised_features.json`

**重要性：** 这是除派生特征敏感性实验外最需要补跑的实验，因为当前Meteorology组包含NIRv、NDVI或SOC参与构造的派生变量，严格意义上并非纯气象输入。

### 3.5 复核SHAP分析

**现有代码：** `SHAP/shap_explanation_and_export.ipynb`

- [ ] 确认SHAP所解释的模型与论文报告的P4最终模型一致。
- [ ] 确认当前Notebook是否在全数据上重新调参和拟合；若是，它与外层LOYO模型并不是同一评价对象。
- [ ] 记录SHAP最终模型的训练数据范围、参数和特征列表。
- [ ] 使用最终确定的特征体系重新运行SHAP。
- [ ] 如果敏感性实验决定删除或修改派生特征，必须重新生成全部SHAP结果。
- [ ] 导出平均绝对SHAP值排序，而不仅保存图。
- [ ] 对前15个变量导出取值与SHAP值的Spearman相关，作为方向性辅助检查。

**必须输出：**

- [ ] `SHAP_Values_Matrix.csv`
- [ ] `SHAP_Feature_Matrix.csv`
- [ ] `SHAP_Metadata_and_Predictions.csv`
- [ ] `SHAP_Mean_Absolute_Importance.csv`
- [ ] `SHAP_Top15_Direction_Summary.csv`
- [ ] 更新后的SHAP条形图和汇总散点图

---

## 4. P1：建议补跑的稳定性分析

### 4.1 按Zone评估P4预测误差

- [ ] 使用P4外层LOYO逐样本预测结果，按Zone计算样本数、MAPE、RRMSE、RMSE、MAE、$R^2$和d-index。
- [ ] 同时按`Year × Zone`计算指标，避免合并结果掩盖特定年份区域失效。
- [ ] 对样本量过小的组合设置最小样本阈值并标记。

**输出：**

- [ ] `P4_Metrics_ByZone.csv`
- [ ] `P4_Metrics_ByYear_Zone.csv`

### 4.2 误差分布与产量区间偏差

- [ ] 按真实产量分位数划分低、中、高产组。
- [ ] 分组计算偏差、MAE、MAPE和RMSE。
- [ ] 计算整体平均误差，检查系统性高估或低估。
- [ ] 输出真实值--预测值散点图、残差图和误差分布图。

**输出：**

- [ ] `P4_Metrics_ByYieldQuantile.csv`
- [ ] `P4_Residual_Summary.csv`
- [ ] 预测散点图与残差图

### 4.3 Bootstrap置信区间

- [ ] 基于外层测试预测结果，对总体MAPE、RRMSE、RMSE和MAE进行Bootstrap。
- [ ] 建议至少1,000次重复，并固定随机种子。
- [ ] 对P4基础模型与P4优化模型的指标差值计算95%置信区间。
- [ ] 对Raw与Full敏感性实验的指标差值计算95%置信区间。

**输出：**

- [ ] `bootstrap_metric_confidence_intervals.csv`
- [ ] `bootstrap_model_difference_confidence_intervals.csv`

---

## 5. P2：计算资源允许时补跑

### 5.1 空间分块验证

**目的：** 检查随机点空间自相关是否使当前跨年误差偏乐观。

- [ ] 按规则网格或空间聚类划分空间块。
- [ ] 保证相邻样本尽量进入同一空间块。
- [ ] 使用空间块进行交叉验证，所有特征处理和训练仅在训练块完成。
- [ ] 与现有LOYO结果比较。

**输出：**

- [ ] `spatial_block_cv_results.csv`
- [ ] `spatial_block_cv_predictions.csv`
- [ ] 空间块分布图

**说明：** 该任务价值较高，但计算和数据组织成本也较高；如果时间不足，可以不作为当前最低完成要求。

### 5.2 留一区域验证

- [ ] 每次留出一个Zone测试，其余Zone训练。
- [ ] 比较模型跨区域外推能力。
- [ ] 记录各Zone样本量差异，防止极端不平衡误导结果。

**输出：**

- [ ] `leave_one_zone_out_results.csv`
- [ ] `leave_one_zone_out_predictions.csv`

---

## 6. 推荐运行顺序

1. [ ] 运行派生特征数值诊断。
2. [ ] 运行Raw、Raw+Cumulative+Delta和Full三组敏感性实验。
3. [ ] 根据前两步决定是否删除、截尾或保留代理特征。
4. [ ] 若特征体系改变，重新运行P1--P5基础实验。
5. [ ] 重新运行最终P4 MOBO优化。
6. [ ] 重新运行修订后的数据源消融实验。
7. [ ] 重新运行多模型比较。
8. [ ] 使用最终模型重新生成SHAP。
9. [ ] 计算按年份、Zone和产量区间的稳定性指标。
10. [ ] 计算Bootstrap置信区间。
11. [ ] 时间允许时运行空间分块或留一区域验证。

---

## 7. 最低必须完成的代码任务

如果时间和计算资源有限，至少完成以下五项：

- [ ] 派生特征分布与极端值诊断。
- [ ] Raw、Raw+Cumulative+Delta、Full三组P4敏感性实验。
- [ ] 修订数据源消融，避免含NIRv、NDVI或SOC的派生变量被称为纯Meteorology输入。
- [ ] 导出P4最终模型逐年指标和逐样本预测。
- [ ] 使用最终确定的特征体系重新运行SHAP。

完成这五项后，才能较有把握地判断现有代理特征是否保留，以及论文关于多源融合和关键变量的结论是否需要调整。# 论文完善与投稿前待办清单

## 1. 当前状态判断

论文已经具备完整的研究主线、方法框架、主要实验结果、讨论和结论，**不需要从头重做**。当前工作的重点是提高方法表述的准确性、补充关键稳健性验证、完善可复现信息，并完成投稿格式与作者信息。

### 已经具备的内容

- [x] 研究问题与30 m冬小麦提前预测目标明确。
- [x] P1--P5分阶段、逐步累积输入的预测框架已经形成。
- [x] 2016--2021年按年份留一验证框架已经形成。
- [x] 基础阶段实验、P4调优、多模型比较、数据源消融和SHAP分析已经写入论文。
- [x] 已将`XGBRFRegressor`改写为“XGBoost框架提供的GPU加速随机森林式回归模型”，不再直接等同于经典随机森林GPU实现。
- [x] 已按实际代码补充基础参数、Optuna TPE多目标优化、特征掩码和嵌套按年份验证流程。
- [x] 消融实验图、SHAP图、结果表和主要参考文献已经存在。

---

## 2. P0：必须优先完成的科学性修正

这些事项直接影响论文结论是否稳妥，建议在继续润色前先完成。

### 2.1 准确定义全部派生特征

- [ ] 在“特征变量构建”中增加公式表或公式组，严格按照`CodeUse/01拼数据+数据处理.ipynb`描述：
  - `WUE = NIRv / (SM + 0.001)`；
  - `Decoupling_Stress = VPD_max / (SM + 0.001)`；
  - `Thermal_Efficiency = NDVI_max / (GDD + 1.0)`；
  - `Hydrothermal_Balance = PPT / Tmean_safe`；
  - 当`Tmean <= 0`时，代码中的`Tmean_safe = 0.1`；
  - `Drought_Vulnerability = VPD / (Clay + 0.1)`；
  - `Fertility_Vigor = NDVI × SOC`；
  - `Cum_GDD`、`Cum_PPT`、`Cum_FDD`和`Cum_VPD`为截至当前阶段的逐阶段累加；
  - `Delta_NDVI`、`Delta_NDWI`和`Delta_SM`为相邻阶段差值。
- [ ] 说明`0.001`、`1.0`、`0.1`是代码中用于避免除零或数值异常的稳定常数，不是农学阈值。
- [ ] 说明派生后代码将正负无穷替换为缺失值，并将缺失值填充为0。
- [ ] 核对Clay、SOC、SM、PPT、VPD、GDD等变量进入公式时的实际单位和数值尺度。

**完成标准：** 论文中的公式、变量名称、单位、异常值处理与Notebook完全一致，其他人能够仅根据论文复现派生过程。

### 2.2 将派生指标统一改称“代理特征”

- [ ] 在方法部分明确声明：上述变量是根据已有数据构建的“经验型复合代理特征”，并非独立测量的标准作物生理指标。
- [ ] 将`WUE`在正文中的解释统一改为“基于NIRv与表层土壤水分构建的水分利用代理特征”或“NIRv--SM比值代理特征”。
- [ ] 将`Decoupling_Stress`解释为“大气干旱需求与表层土壤水分共同构成的复合胁迫代理特征”。
- [ ] 将`Thermal_Efficiency`解释为“冠层峰值长势相对于阶段积温的热量响应代理特征”。
- [ ] 将`Hydrothermal_Balance`解释为“降水与温度组合代理特征”，不能等同于严格水热平衡。
- [ ] 将`Drought_Vulnerability`解释为“VPD与黏粒含量组合代理特征”，不能等同于实际抗旱能力。
- [ ] 将`Fertility_Vigor`解释为“SOC背景与NDVI长势的交互项”，不能等同于实际土壤肥力。
- [ ] 摘要、结果、SHAP解释、讨论和结论中的相关术语同步修改，避免前后不一致。

**完成标准：** 全文不再把自定义比值或交互项直接表述为真实WUE、真实干旱脆弱性或严格生理效率。

### 2.3 收缩SHAP结论的因果和机理表述

- [ ] 将“P4_WUE表明水分利用效率提高产量”改为“较高的NIRv--SM组合代理值与模型较高预测值相关”。
- [ ] 将“关键驱动因子”在必要处改为“模型关键判别变量”或“重要预测特征”。
- [ ] 保留“SHAP不等于因果推断”的限制说明，并在结果段首次解释代理特征时再次提示。
- [ ] 对`Cum_VPD_P4`高值出现正SHAP的现象维持谨慎解释，不直接得出VPD促进增产的结论。
- [ ] 避免使用“验证了某变量的农学生理机制”等过强表述，改为“与已有农学认识具有一定一致性”。

**完成标准：** SHAP仅用于解释模型如何利用变量，不用于证明变量的独立因果效应。

### 2.4 增加派生特征敏感性实验

建议至少完成一个“原始特征 vs. 原始+派生特征”的P4对照，这是目前最能增强信心的补充实验。

- [ ] 固定同一按年份留一验证划分，建立仅使用原始特征的模型。
- [ ] 建立加入全部派生代理特征的模型。
- [ ] 保持模型类型、参数搜索预算、随机种子和评价指标一致。
- [ ] 比较MAPE、RRMSE、RMSE、MAE、$R^2$和d-index。
- [ ] 最好分别报告各测试年份结果和所有年份合并结果。
- [ ] 如果派生特征提升很小或无提升，考虑从主模型删除理论依据最弱的特征。
- [ ] 如果派生特征有稳定提升，将其作为“经验型特征工程有效性”的证据，而不是作为生理指标有效性的证据。

**完成标准：** 能够回答“这些派生特征是否确实改善跨年预测，而不是只在SHAP中显得重要”。

### 2.5 对高风险派生特征进行单独检查

- [ ] 检查`WUE=NIRv/(SM+0.001)`的分布、极端值和各年份分位数。
- [ ] 检查`Hydrothermal_Balance=PPT/Tmean_safe`在`Tmean<=0`样本中的数量和极端值。
- [ ] 检查`Drought_Vulnerability=VPD/(Clay+0.1)`是否受Clay单位和缩放方式支配。
- [ ] 检查`Cum_VPD`是否只是阶段VPD均值的相加；若是，应称“阶段VPD累加代理量”，不能称严格时间积分。
- [ ] 对派生特征进行1%和99%分位数、最大最小值及异常样本检查。
- [ ] 如存在极端值，决定采用截尾、标准化、删除该特征或仅在局限性中说明，并保证重新训练结果与论文一致。

**完成标准：** 确认模型结果不是由除法公式产生的少量极端值主导。

---

## 3. P1：增强论文可信度的关键工作

### 3.1 核对模型名称和实验身份

- [ ] 全文统一区分：经典`RandomForestRegressor`、`XGBRFRegressor`随机森林式模型和`XGBRegressor`梯度提升模型。
- [ ] 核对基础P1--P5实验实际使用的是哪一个类。
- [ ] 核对数据源消融实验实际使用的是哪一个类；如果使用scikit-learn随机森林，应明确写“普通随机森林”。
- [ ] 核对P4调优、模型比较和SHAP解释分别对应哪个最终模型文件和参数。
- [ ] 将“RF+MOBO”首次出现时给出完整定义。

### 3.2 保存并报告最终模型配置

- [ ] 从每个外层测试年份导出最佳超参数、入选特征数量和入选特征列表。
- [ ] 报告不同年份最佳参数是否差异较大。
- [ ] 说明最终汇总指标来自六个独立测试年份预测值合并计算，还是六年指标平均。
- [ ] 保存随机种子、XGBoost版本、Optuna版本、GPU设备和主要运行环境。
- [ ] 确认SHAP使用的是哪个外层模型、重训模型或全样本模型，并在论文中写明。

### 3.3 完善数据预处理细节

- [ ] 写清楚导出值`-9999`在建模前如何识别和处理。
- [ ] 写清楚缺失值填0发生在什么阶段、哪些变量受影响、是否添加缺失标记。
- [ ] 写清楚异常样本和产量异常值是否筛除，以及筛除阈值。
- [ ] 写清楚不同传感器特征拼接、MODIS填补和重采样过程的优先顺序。
- [ ] 写清楚Landsat 8与Sentinel-2跨传感器校准公式或参数来源。
- [ ] 明确ERA5-Land和MODIS等粗分辨率数据匹配到30 m点位不等于获得真实30 m气象信息。

### 3.4 核对时间窗和分区代码

- [ ] 将表中的P1--P5时间范围与`CodeUse/00分区对齐.js`逐项核对。
- [ ] 给出跨区省份使用的具体纬度阈值。
- [ ] 核对Zone 1--5编号与论文中的区域名称完全一致。
- [ ] 检查所有年份跨年时间窗口是否正确处理年份转换。
- [ ] 明确P4相对于各分区典型收获日期大约提前多少天或多少周。
- [ ] 如果无法精确计算统一提前量，按分区报告范围，避免笼统称“提前两个月”。

### 3.5 强化验证设计说明

- [ ] 明确每年20,000个样本是否在建模前因缺失值而减少，并报告最终样本数。
- [ ] 报告六个年份各自的样本数、产量均值、标准差和范围。
- [ ] 说明每年随机抽样点不同，因此留一年验证属于时间外推，不是同一地块跨年验证。
- [ ] 讨论随机点的空间自相关可能使总体误差偏乐观。
- [ ] 如计算资源允许，增加空间分块验证或按区域留一验证；若不增加，必须列为局限性。

### 3.6 核对所有结果的来源

- [ ] 为基础P1--P5结果保留对应CSV或JSON。
- [ ] 为P4调优前后结果保留逐年预测和总体指标文件。
- [ ] 为多模型比较保留各模型逐年结果、参数和预测值。
- [ ] 为消融实验保留特征清单、逐年指标和总体指标。
- [ ] 为SHAP图保留生成脚本、特征顺序和模型来源。
- [ ] 逐项核对论文表格中的数字是否与结果文件一致，避免人工转录错误。

---

## 4. P1：论文内容需要补充或改写的部分

### 4.1 摘要

- [ ] 将P4_WUE改为代理特征表述。
- [ ] 避免“进一步验证应用潜力”等过强表述，可改为“表明其具有进一步业务评估价值”。
- [ ] 明确调优模型为`XGBRFRegressor`随机森林式模型，而不是严格等同于经典随机森林。
- [ ] 如完成派生特征敏感性实验，在摘要中仅保留最关键的一句结果。

### 4.2 引言

- [ ] 增加高分辨率产量标签、阶段预测、跨年泛化和可解释机器学习方面的近期研究。
- [ ] 当前参考文献数量较少，应重点补充方法来源，而不是单纯增加背景性引用。
- [ ] 避免把“随机森林通常采用默认参数”写成普遍性过强的判断，需有文献支持或弱化措辞。

### 4.3 数据与方法

- [ ] 增加派生代理特征公式表。
- [ ] 增加最终输入特征分类表，区分原始、累积、阶段差分和经验复合代理特征。
- [ ] 增加模型和验证流程图，展示数据提取、分区对齐、P1--P5累积、外层LOYO、内层优化和独立测试。
- [ ] 补充所有评价指标公式，特别是RRMSE分母、MAPE零值处理和d-index定义。
- [ ] 写明所有模型的具体软件包、类名和版本。
- [ ] 写明多模型比较中MLR、SVR和XGBoost的实际搜索空间。

### 4.4 结果

- [ ] 增加各年份误差，而不仅是合并总体误差，以展示跨年稳定性。
- [ ] 对P4与P5差异使用谨慎表述；没有显著性检验时，不称“显著优于”。
- [ ] 如可能，对逐样本绝对误差或逐年指标进行配对检验或Bootstrap置信区间。
- [ ] 增加观测值--预测值散点图或密度图，展示高低产区间的系统偏差。
- [ ] 增加残差分布或按Zone分组误差，检查区域性偏差。
- [ ] 完成派生特征敏感性实验后增加一张简洁对比表。

### 4.5 讨论

- [ ] 区分“预测性能证据”和“农学机理解释证据”。
- [ ] 讨论30 m产量标签自身的不确定性可能限制模型上限。
- [ ] 讨论粗分辨率气象与MODIS数据匹配到30 m样本后仍具有空间尺度不一致。
- [ ] 讨论分区固定日历窗口无法反映逐年和逐地块真实物候变化。
- [ ] 讨论代理特征公式、稳定常数、量纲和极端值风险。
- [ ] 讨论空间随机抽样与空间自相关问题。
- [ ] 避免将较低MAPE直接等同于已经达到实际业务部署条件。

### 4.6 结论

- [ ] 保留多源融合、P4提前预测和跨阶段信息累积三项核心结论。
- [ ] 将“水分利用效率”等改为“水分相关代理特征”。
- [ ] 将“关键驱动因子”改为“模型重要预测特征”。
- [ ] 不在结论中新增正文未验证的机理判断。
- [ ] 如派生特征敏感性实验不支持增益，删除对相关代理特征的突出强调。

---

## 5. P2：图表与可读性完善

### 必要图件

- [ ] 研究区与五个Zone空间分布图。
- [ ] 数据处理和模型验证流程图。
- [ ] P1--P5阶段时间轴示意图，标明不同Zone的近似差异。
- [ ] P1--P5基础模型误差变化图。
- [ ] P4调优前后对比图。
- [ ] 观测值与预测值散点图。
- [ ] 按年份或Zone的误差对比图。

### 现有图件检查

- [ ] 检查消融实验图字体、单位、图例和论文语言是否统一。
- [ ] 检查SHAP图中的英文特征名称是否需要增加中文说明。
- [ ] 确认SHAP条形图和散点图使用完全相同的模型与数据。
- [ ] 确认所有图达到目标期刊分辨率和尺寸要求。
- [ ] 避免把PDF预览渲染图保存在项目目录；预览文件应放在`/tmp`。

### 表格检查

- [ ] 数据源表补充原始空间和时间分辨率。
- [ ] 物候窗口表与代码日期一致。
- [ ] 所有指标表统一有效数字和单位。
- [ ] 表注说明总体指标的汇总方式。
- [ ] 对最优值使用统一加粗规则，避免选择性突出。

---

## 6. P2：参考文献与引用

- [ ] 为XGBoost和`XGBRFRegressor`补充官方论文或官方文档来源。
- [ ] 为Optuna、TPE和多目标优化补充方法引用。
- [ ] 为SHAP补充原始方法论文。
- [ ] 为MAPE、RRMSE和d-index定义补充权威来源或清晰公式。
- [ ] 为NIRv、NDWI、EVI、VPD和GDD补充变量来源或经典文献。
- [ ] 为ChinaWheatYield30m核对数据集作者、年份、DOI和版本。
- [ ] 核对`ji2026multisourcephenology`是否已经正式发表；若尚未正式发表，按实际出版状态著录。
- [ ] 检查BibTeX中的题名大小写、期刊名、卷期、页码、DOI和URL。
- [ ] 删除正文未引用的文献，补齐正文已引用但BibTeX缺失的文献。

---

## 7. P2：可复现材料整理

- [ ] 为每个Notebook增加顶部说明：输入文件、输出文件、运行顺序和主要参数。
- [ ] 固定推荐运行顺序：分区对齐 → 拼接与特征工程 → 静态变量筛选 → 基础实验 → P4优化 → 多模型比较 → 消融 → SHAP。
- [ ] 清理Notebook中的无关测试单元、硬编码本机路径和重复代码。
- [ ] 将随机种子统一记录。
- [ ] 保存Python和关键库版本。
- [ ] 建立结果文件与论文表图的对应关系说明。
- [ ] 确认代码中不存在测试年份参与特征筛选、标准化拟合或参数优化的泄漏。
- [ ] 如数据无法公开，写明数据获取地址、处理步骤和受限原因。

---

## 8. P3：投稿前信息与格式

- [ ] 填写作者姓名。
- [ ] 填写作者单位、城市和邮编。
- [ ] 填写通讯作者邮箱。
- [ ] 删除或按期刊要求填写Received、Revised、Accepted和Published占位信息。
- [ ] 将“Remote Sensing风格排版草稿”替换为正式期刊模板或删除。
- [ ] 补充Author Contributions。
- [ ] 补充Funding。
- [ ] 补充Data Availability Statement。
- [ ] 补充Conflicts of Interest。
- [ ] 如涉及生成式AI辅助写作，按目标期刊政策决定是否声明。
- [ ] 按目标期刊要求补充缩略语表、附录或补充材料。
- [ ] 检查中英文标点、数字与单位空格、变量斜体和缩写首次定义。
- [ ] 完整运行XeLaTeX/Biber编译，处理未定义引用、缺图、表格溢出和Overfull box。
- [ ] 通读最终PDF，检查分页、浮动体位置、图中文字和参考文献格式。

---

## 9. 推荐执行顺序

1. [ ] 按代码补全派生特征公式和单位。
2. [ ] 全文将自定义指标降级为经验型代理特征。
3. [ ] 检查派生特征分布、极端值和异常值处理。
4. [ ] 运行“仅原始特征 vs. 加入派生特征”敏感性实验。
5. [ ] 根据敏感性结果决定保留、删除或弱化哪些派生特征。
6. [ ] 更新摘要、SHAP结果、讨论和结论。
7. [ ] 核对模型类、参数、验证流程和所有结果文件。
8. [ ] 补充逐年/分区误差与必要图件。
9. [ ] 补充方法类参考文献和可复现信息。
10. [ ] 完成作者信息、期刊格式和最终编译检查。

---

## 10. 最低可投稿版本的完成标准

满足以下条件后，可以认为论文达到较稳妥的投稿准备状态：

- [ ] 所有派生特征有准确公式、单位、稳定常数和代理性质说明。
- [ ] 已完成派生特征敏感性实验，且结论按实验结果调整。
- [ ] SHAP解释不包含未经验证的因果推断。
- [ ] 模型类名、参数、验证流程与代码完全一致。
- [ ] 所有表格数字均可追溯到保存的结果文件。
- [ ] 至少包含研究区图、流程图、阶段结果图、预测散点图、消融图和SHAP图。
- [ ] 数据、代码和运行环境具备基本可复现说明。
- [ ] 参考文献覆盖数据源、算法、优化、解释方法和关键变量定义。
- [ ] 作者信息、声明和期刊格式全部完成。
- [ ] 最终PDF无缺图、缺引、明显排版错误或占位文本。

## 11. 当前最重要的判断

当前论文的核心框架和已有预测结果并未因代理特征命名问题而失效。最重要的不是推倒重来，而是通过“准确命名 + 公式披露 + 极端值检查 + 敏感性实验”确定这些派生特征在模型中的真实作用，再据此收缩或保留解释。完成这一步后，论文的可信度会明显提高。